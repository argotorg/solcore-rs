data Option = None | Some(word);

function fromOption(x: Option) -> word {
  match x {
  | .Some(v) => return v;
  | .None => return 0;
  }
}

function main() -> word {
  return fromOption(Option.Some(3));
}
