import std.{*};
import std.dispatch.{*};
contract Counter {
  counter : uint256;
  
  constructor() {
    counter = 41;
  }
  
  public function test() -> uint256 {
    counter = counter + 1;
    return counter;
  }
}
