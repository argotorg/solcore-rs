enum Foo<a> { Foo(word) }
  function wrap<a>(x: word) returns (Foo<a>) {
    return Foo.Foo(x);
  }

  function unwrap() returns (word) {
    match (wrap(42)) { case Foo.Foo(w) { return w; } }
  }

  contract C {
    function main() public returns (word) {
      return unwrap();
    }
  }
