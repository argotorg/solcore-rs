data Option = None | Some(word);

function mkSome(x: word) -> Option {
  return .Some(x);
}

function main() -> word {
  match mkSome(7) {
  | Option.Some(v) => return v;
  | Option.None => return 0;
  }
}
