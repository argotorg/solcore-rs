import std; // migrate-syntax: keep-legacy-negative
import std.dispatch;

contract BadFallback {
    constructor() {}

    fallback(x: uint256) returns () {
        revert("fallback-was-called");
    }
}
