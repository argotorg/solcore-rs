import std;
import std.dispatch;

// The Classic prefix modifier order is no longer accepted.
// migrate-syntax: keep-legacy-negative
public function answer() -> uint256 {
    return uint256.uint256(42);
}
