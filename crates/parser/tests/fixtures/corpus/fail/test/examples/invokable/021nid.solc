contract Id1 {
  function id(x) public {
    return x ;
  }

  function nid() public {
    return id;
  }

  function const(x, y) public { return x; }

  function main() public {
    return nid(42);
  }
}
