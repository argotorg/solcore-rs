import std; // migrate-syntax: keep-legacy-negative
import std.dispatch;

contract PublicFallback {
    constructor() {}

    fallback() public returns () {
        revert("fallback-was-called");
    }
}
