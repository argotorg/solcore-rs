contract Id1 {
  function id(x) {
    return x ;
  }

  function nid() {
    return id;
  }

  function const(x, y) { return x; }

  function main() {
    let f = nid();
    return const(f(42), id(1));
  }
}
