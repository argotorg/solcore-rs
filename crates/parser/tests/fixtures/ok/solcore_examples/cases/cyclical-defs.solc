function foo(x : word) -> word {
  return bar(x);
}
function bar(x : word) -> word {
  return foo(x);
}

contract C {
  function m(x : word) -> word {
    return n(x);
  }
  function n(x : word) -> word {
    return m(x);
  }
  function main() -> word {
    return m(1);
  }
}
