forall self.
class self:C {
    function size(x:self) -> word;
}

instance ():C {
    function size(x:()) -> word {
        return 0;
    }
}

instance uint:C {
    function size(x:uint) -> word {
        return 1;
    }
}
