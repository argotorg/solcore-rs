data Option = None | Some(word);

function mkSome(x: word) -> Option {
  return .Some(x);
}

function fromOption(x: Option) -> word {
  match x {
  | .Some(v) => return v;
  | .None => return 0;
  }
}
