function zero() returns (word) {
  return 0;
}

function apply(f: function(word) returns (word), x: word) returns (word) {
  return f(x);
}

function choose(c: bool, a: word, b: word) returns (word) {
  return c ? a : b;
}

function keepThen(then: word) returns (word) {
  return then;
}
