alias CompileTime = word;

contract ComptimeModifier {
  function f(comptime x: word) returns (comptime word) {
    return x;
  }

  function identifier(x: CompileTime) returns (CompileTime) {
    let compileTimeValue: word = 1;
    let comptime y: word = f(compileTimeValue);
    return y;
  }
}
