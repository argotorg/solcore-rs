data Light = On | Off;
data Power = Plugged | Battery;

function isOn(l: Light) -> word {
  match l {
  | Light.On => return 1;
  | Light.Off => return 0;
  }
}

function main() -> word {
  return isOn(On);
}
