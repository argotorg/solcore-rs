// Mirrors reference corpus test/examples/cases/fallback-with-args.solc
// (expected failure there): fallback must take no arguments.
import std;
import std.dispatch;

contract BadFallback {
    constructor() {}

    fallback(x: uint256) returns (()) {
        revert;
    }
}
