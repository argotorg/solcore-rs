// mstore does not return a value, so it cannot be assigned.
contract Test {
  function main() public returns (word) {
    let x: word;
    assembly {
      x := mstore(1, 1)
    }
    return x;
  }
}
