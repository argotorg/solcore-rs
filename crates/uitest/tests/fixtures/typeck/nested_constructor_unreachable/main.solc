enum Inner { A, B };
enum Outer { Other, Wrap(Inner) };

function pick(x: Outer) returns (word) {
  match (x) {
  case Outer.Wrap(_) { return 0; }
  case Outer.Wrap(Inner.A) { return 1; }
  case Outer.Other { return 2; }
  }
}
