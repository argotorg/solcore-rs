function add3(a: word, b: word, c: word) -> word {
  return a;
}

function f(x: word) -> word {
  return add3(add3(x, x, add3(x, add3(x, x, x), x)),
              add3(x, x, add3(x, true, x)),
              x);
}
