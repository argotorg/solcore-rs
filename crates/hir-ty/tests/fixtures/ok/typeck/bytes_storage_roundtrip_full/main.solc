import std;

contract C {
  value : bytes;

  constructor(x: bytes memory) {
    value = x;
  }

  function get() public returns (bytes memory) {
    return value;
  }

  function main() returns () {
    return ();
  }
}
