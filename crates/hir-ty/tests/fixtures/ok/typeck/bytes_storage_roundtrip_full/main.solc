import std.{*};

contract C {
  value : bytes;

  constructor(x : memory(bytes)) {
    value = x;
  }

  public function get() -> memory(bytes) {
    return value;
  }

  function main() -> () {
    return ();
  }
}
