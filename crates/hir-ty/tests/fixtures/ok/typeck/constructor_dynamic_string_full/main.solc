import std;

contract C {
  value : string;

  constructor(x: string memory) {
    value = x;
  }

  function get() public returns (string memory) {
    return value;
  }

  function main() returns () {
    return ();
  }
}
