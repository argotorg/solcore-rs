import std.{*};

contract C {
  value : string;

  constructor(x : memory(string)) {
    value = x;
  }

  public function get() -> memory(string) {
    return value;
  }

  function main() -> () {
    return ();
  }
}
