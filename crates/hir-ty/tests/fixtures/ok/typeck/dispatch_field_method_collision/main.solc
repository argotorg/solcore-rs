import std.{*};
import std.dispatch.{*};

contract C {
  data C = Foo;

  allowance: uint256;

  public function allowance() -> uint256 {
    return allowance;
  }

  public function Foo() -> uint256 {
    return 1;
  }
}
