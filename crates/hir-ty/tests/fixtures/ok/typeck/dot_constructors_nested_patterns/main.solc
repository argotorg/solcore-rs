enum Option { None, Some(word) }

function mkSome(x: word) returns (Option) {
  return Option.Some(x);
}

function fromOption(x: Option) returns (word) {
  match (x) { case Option.Some(v) { return v; } case Option.None { return 0; } }
}
