contract Triple {

  function asel(t : (word, word, word)) -> word {
    match t {
      | (a,b,c) => return c;
    }
  }

  // #[() -> 42]
  public function run() -> word {
    return asel((1,21,42));
  }
}
