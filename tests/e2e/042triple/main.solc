import std;
import std.dispatch;

contract Triple {

  function asel(t: (word, word, word)) returns (word) {
    match (t) { case (a, b, c) { return c; } }
  }

  // #[() -> 42]
  function run() public returns (uint256) {
    return uint256.uint256(asel((1,21,42)));
  }
}
